# ===========================================
# CONFIGURAÇÃO DE AMBIENTE - DG APP
# ===========================================
# Copie este arquivo para .env e configure os valores apropriados

# ===========================================
# BANCO DE DADOS
# ===========================================
# String de conexão do MySQL
# Formato: mysql://usuario:senha@host:porta/database
DATABASE_URL="mysql://usuario:senha@localhost:3306/dg_contracts"

# ===========================================
# AUTENTICAÇÃO
# ===========================================
# Chave secreta para assinatura de tokens JWT
# IMPORTANTE: Use uma chave forte e única em produção
JWT_SECRET="sua_chave_secreta_jwt_muito_segura_aqui_123456789"

# ===========================================
# SERVIDOR
# ===========================================
# Porta do servidor (padrão: 3000)
PORT=3000

# Ambiente de execução (development, production, test)
NODE_ENV=development

# Host do servidor (padrão: 0.0.0.0 para Docker)
HOST=0.0.0.0

# ===========================================
# UPLOAD DE ARQUIVOS
# ===========================================
# Tamanho máximo de arquivo em bytes (padrão: 10MB)
MAX_FILE_SIZE=10485760

# ===========================================
# CRIPTOGRAFIA DE ARQUIVOS
# ===========================================
# Senha para criptografia de arquivos
# IMPORTANTE: Use uma senha forte e única em produção
FILE_ENCRYPTION_PASSWORD="sua_senha_de_criptografia_muito_segura_aqui_mude_em_producao_123456789"

# ===========================================
# RATE LIMITING
# ===========================================
# Janela de tempo para rate limiting em ms (padrão: 15 minutos)
RATE_LIMIT_WINDOW_MS=900000

# Número máximo de requests por janela (padrão: 100)
RATE_LIMIT_MAX_REQUESTS=100

# Número máximo de tentativas de autenticação (padrão: 5)
RATE_LIMIT_AUTH_MAX_REQUESTS=5

# ===========================================
# FRONTEND
# ===========================================
# URL do frontend para configuração de CORS
FRONTEND_URL=http://localhost:3000

# ===========================================
# AWS (Para produção)
# ===========================================
# Estas variáveis são gerenciadas pelo AWS Secrets Manager em produção
# Não configure aqui para produção - use apenas para desenvolvimento local

# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key
